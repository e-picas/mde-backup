; This file was automatically generated with PHP_Extended_Markdown_Builder class on {%DATE%}
;
; $emd_config = array(
; ...
; );
;

; Change to ">" for HTML output
empty_element_suffix=" />"

; Define the width of a tab (4 spaces by default)
tab_width=4

; Table of hash values for escaped characters:
escape_chars="\`*_{}[]()>#+-.!:|"

; Regex to match balanced [brackets].
; Needed to insert a maximum bracked depth while converting to PHP.
nested_brackets_depth=6

; Regex to match balanced (parenthesis).
; Needed to insert a maximum bracked depth while converting to PHP.
nested_url_parenthesis_depth=4

; These are done on instance creation (nothing transmitted)
gamutsList[]= initGamut
; These are done before transforming (text not transmitted)
gamutsList[]= setupGamut
; These are very first in transforming (text transmitted)
gamutsList[]= initTransformGamut
; These are first executed commands (text transmitted)
gamutsList[]= documentGamut
; These are all the transformations that occur *within* block-level tags like paragraphs, headers, and list items. (text transmitted)
gamutsList[]= spanGamut
; These are all the transformations that form block-level tags like paragraphs, headers, and list items. (text transmitted)
gamutsList[]= blockGamut
; Special Gamut (text transmitted)
gamutsList[]= htmlBlockGamut
; These are done after transforming (text not transmitted)
gamutsList[]= teardownGamut

documentGamut[runGamut:blockGamut]=50

htmlBlockGamut[runGamut:blockGamut]=2

{%CONFIG%}